export interface ImprintType {
  sections: [
    {
      id: number;
      title: string;
      text: string;
    },
  ];
}

export interface PolicyType {
  sections: [
    {
      title: string;
      text: string;
    },
  ];
}
